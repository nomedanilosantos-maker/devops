name: CI DevOps Lab

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run basic commands
        run: |
          echo "CI pipeline a correr com sucesso"
          uname -a
          ls -l
